import TicketList from '../components/user/TicketList';

const UserDashboard = () => {
    return (
        <div>
            <h1>User Dashboard</h1>
            <TicketList />
        </div>
    );
};

export default UserDashboard;
